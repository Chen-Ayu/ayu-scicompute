# Materials Studio DMol3 prepared-only example

This synthetic manifest tests independent MaterialsScript rendering. No XSD or vendor software is bundled. Execution requires a user-authorized Materials Studio installation and a user-supplied `water.xsd` in the run directory.
